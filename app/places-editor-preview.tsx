import PlacesEditor from '@/components/today/editors/PlacesEditor';

// Throwaway screen for visually checking the Places editor. Not part of the app flow.
export default function PlacesEditorPreview() {
  return <PlacesEditor onClose={() => {}} />;
}
